name: Verify Application Startup

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  run-application-check:
    runs-on: ubuntu-latest

    
      

    steps:
    
      - name: ✅ Debug Print Environment & Keys (safe)
        run: |
          echo "$RSA_PUBLIC_KEY" | base64 --decode > key.pem
          echo "Decoded key:"
          cat key.pem

  
          
