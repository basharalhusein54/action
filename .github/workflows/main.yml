name: Verify Application Startup

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  run-application-check:
    runs-on: ubuntu-latest

    env:
      RSA_PUBLIC_KEY: ${{ secrets.RSA_PUBLIC_KEY}}
      

    steps:
     

      - name: ✅ Debug Print Environment & Keys (safe)
        run: |
          echo "First 100 chars of RSA_PUBLIC_KEY: ${RSA_PUBLIC_KEY:0:100}"

  
          
